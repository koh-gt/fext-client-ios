<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  FEXT Info.plist additions. scripts/patch_xcode.py merges these keys into the
  kivy-ios-generated <App>-Info.plist. Identity keys (CFBundleIdentifier,
  versions, CFBundleDisplayName) are set separately from the build CLI.

  ATS / transport: as of v7 the pinned relay is HTTPS/WSS, but still on a bare
  IP (https://118.189.201.104:50607), not a domain. ATS cannot scope an
  exception to an IP literal, and its handling of certificates issued for an IP
  is historically brittle. FEXT's own networking is Python sockets (requests +
  websocket-client verifying against the shipped certifi bundle), which bypass
  ATS entirely, so this key does not affect FEXT's connectivity either way — it
  is kept only as belt-and-suspenders for the bare-IP endpoint. Every byte on
  the wire is TLS AND already E2E ciphertext underneath.
  >>> For public App Store review, move the relay to a real domain, then DELETE
  >>> the NSAppTransportSecurity key below. See README "App Store notes".

  Export compliance (ITSAppUsesNonExemptEncryption) is deliberately NOT set
  here — it is a legal declaration the publisher must make. See README.
-->
<plist version="1.0">
<dict>
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<true/>
	</dict>
	<key>UIRequiresFullScreen</key>
	<true/>
	<key>UIStatusBarStyle</key>
	<string>UIStatusBarStyleLightContent</string>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<false/>
</dict>
</plist>
